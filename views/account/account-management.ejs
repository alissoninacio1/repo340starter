<div class="account-management-container-view">
    <div class="container">
        <h1>Welcome to your account management page!</h1>
        <% if (typeof messages !== 'undefined' && messages.length > 0) { %>
            <div class="flash-messages">
                <% messages.forEach(function(message) { %>
                    <div class="flash-message">
                        <%= message %>
                    </div>
                <% }); %>
            </div>
        <% } %>
        <% if (typeof errors !== 'undefined' && errors.length > 0) { %>
            <div class="error-messages">
                <% errors.forEach(function(error) { %>
                    <div class="error-message">
                        <%= error %>
                    </div>
                <% }); %>
            </div>
        <% } %>
        <p>You're logged in.</p>
    </div>
</div>


<!---- css to be added later--->

<!-- /* Responsive styles for account management view */
.account-management-container-view {
    padding: 20px;
}

.container {
    max-width: 800px;
    margin: 0 auto;
}

h1 {
    text-align: center;
}

.flash-messages,
.error-messages {
    margin-top: 20px;
}

.flash-message,
.error-message {
    padding: 10px;
    margin-bottom: 10px;
}

/* Responsive styles for mobile */
@media (max-width: 600px) {
    .container {
        padding: 0 10px;
    }

    h1 {
        font-size: 24px;
    }
} -->

 